<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Sistem De Gestionare a Ciclului de Viata a unui Produs" Height="450" Width="800 "
        xmlns:UserControls="clr-namespace:UI.UserControls"
        SizeChanged="Window_SizeChanged"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        WindowStyle="None">
    <Window.Background>
        <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
            <GradientStop Color="#00b894" Offset="0" />
            <GradientStop Color="#00b894" Offset="1" />
        </LinearGradientBrush>
    </Window.Background>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.06*"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--Window Buttons-->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Vertical">
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button x:Name="btnMinimize"  Click="btnMinimize_Click" Width="20" Height="20" Margin="10,0,10,0"  Background="Transparent" Foreground="WhiteSmoke" Content="-" Padding="10" FontSize="15" BorderThickness="0" Style="{StaticResource TransparentButton}"/>
                <Button x:Name="btnMaximize"  Click="btnMaximize_Click" Width="20" Height="20" Margin="10,0,10,0"  Background="Transparent" Foreground="WhiteSmoke" Content="⏹️" Padding="10" FontSize="15" BorderThickness="0" Style="{StaticResource TransparentButton}"/>
                <Button x:Name="btnClose"  Click="btnClose_Click" Width="20" Height="20" Margin="10,0,5,0"  Background="Transparent" Content="❌" Foreground="WhiteSmoke" Padding="10" FontSize="15" BorderThickness="0" Style="{StaticResource TransparentButton}"/>
            </StackPanel>
          
        </Grid>
        <!--Window Buttons-->
        
        <!--Background-->
        <Border Grid.Row="0" Grid.RowSpan="2" BorderBrush="DarkMagenta" Opacity="0.98" BorderThickness="0" CornerRadius="35,35,0,0" Margin="0,25,0,0">
            <Border.Background>
                <RadialGradientBrush RadiusX="3" RadiusY="0.8" Opacity="1">
                    <GradientStop Color="#2d3436" Offset="0" />
                    <GradientStop Color="#636e72" Offset="1" />
                </RadialGradientBrush>
            </Border.Background>
            <Border Margin="60,0,0,0" BorderBrush="DarkMagenta" Opacity="0.98" BorderThickness="0" CornerRadius="0,35,0,35">
                <Border.Background>
                <RadialGradientBrush RadiusX="3" RadiusY="0.8" Opacity="1">
                    <GradientStop Color="AliceBlue" Offset="0" />
                    <GradientStop Color="WhiteSmoke" Offset="1" />
                </RadialGradientBrush>
                </Border.Background>
            </Border>
        </Border>
        <!--Background-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Stretch" HorizontalAlignment="Center">
                <Button Height="50" Width="50" Margin="5" Style="{StaticResource TransparentButton}">💾</Button>
                <Button Height="50" Width="50" Margin="5" Style="{StaticResource TransparentButton}">💾</Button>
                <Button Height="50" Width="50" Margin="5" Style="{StaticResource TransparentButton}">💾</Button>
                <Button Height="50" Width="50" Margin="5" Style="{StaticResource TransparentButton}">💾</Button>
                <Button Height="50" Width="50" Margin="5" Style="{StaticResource TransparentButton}">💾</Button>
                <Button Height="50" Width="50" Margin="5" Style="{StaticResource TransparentButton}">💾</Button>
                <Button Height="50" Width="50" Margin="5" Style="{StaticResource TransparentButton}">💾</Button>
            </StackPanel>
            <Frame x:Name="MainFrame" Grid.Column="1" Padding="5"/>
        </Grid>
    </Grid>
</Window>
